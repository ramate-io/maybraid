use crate::frond_render_params::FrondCrownRenderParams;

use super::RenderHelper;

/// Renders a standalone frond crown into the scene.
pub type FrondCrownRenderHelper = RenderHelper<FrondCrownRenderParams>;
