# Contributing (`chico-forests`)

Forests assemble **concrete grove types** from `chico-groves`. Do not add a `TuftsGrove` / `UpperCanopyGrove` enum in groves.

- Forest cell: [`DEFAULT_FOREST_EXTENT_XZ`](src/extent.rs) (1600 m).
- Grove tile: [`DEFAULT_FOREST_GROVE_TILE_XZ`](src/extent.rs) (100 m, same as grove preview).
- Hopscotch: [`chico_hopscotch`](src/chico.rs) ([RFC-183 §3.5.5](../../../rfc/rfc-000-000-183-chico-vegetation/03-05-cellular-forests/05-chico-vegetation/README.md)).
- Layer recipes: [`layerings.rs`](src/layerings.rs). Ground cover is omitted. Conifer Lower Massives is missing — drop that bucket; do not alias Alpine or Conifer Massives.
- Grow with `Params::default().with_extent(tile).build_on(world)`. Do not pass CLI grove noise into `build_unit`.
- Planting cells are **world-aligned** ([`GroveExtent::cells_overlapping`](../groves/src/grove/extent.rs)). Adjacent tiles of the same grove share one lattice; a tile owns a cell when the cell center is in its half-open XZ footprint.
- Every presenting 100 m tile softmax-blends a cardinal run of **produced grove** slots ([`blend.rs`](src/blend.rs), radius 8). Each slot's kind is whoever produced that grove. Same-kind slots share one logit (best influence) so a uniform block does not drown the seam. Interior tiles whose slots are all one planted kind skip softmax and grow that recipe once. Empty layers still present neighbor islands. Stream cache is still adjacent forest selections. See [`presenting_recipes`](src/assemble.rs) + [`NeighborLayers`](src/assemble.rs).
- Do not implement the full RFC `ForestGroveBiases` set yet. `select_cell` leaves grove biases at default.
- [`ChicoForest`](src/forest.rs) is a **select-only** generation dependency (`SelectedLayers` on a 1600 m cell). Do not implement `LodScene` on it. Do not bake grove tiles in `build_with_id`.
- Canopy bump-outs read [`BumpOutSelection`](src/bump_out.rs) on [`ForestGroveKind`](src/kind.rs) (authored density / bite / height / palette). Sample the **highest occupied layer**. Blend overlapping 100 m tiles onto the 160 m terrain cell; skip empty tiles so they do not dilute occupied neighbors. Do not grow groves for bump-outs. [`CanopyBumpOut`](src/bump_out.rs) `build_with_id` `get_or_generate`s overlapping [`ChicoForest`](src/forest.rs) cells, then stores the blended 3×3 neighborhood.
- [`ChicoGrove`](src/grove.rs) is the generate origin: one id per (100 m tile, layer). `build_with_id` `get_or_generate`s the parent forest (and cardinal neighbors), then stores blend recipes. Growing plants is presentation (or a later off-thread fulfill), not generate.
- Forest present spawns [`ChicoGroveHost`](src/host.rs) (`ForestGroveTile` + [`ForestLayer`](src/kind.rs)), not the concrete grove type ([#652](https://github.com/ramate-io/maybraid/issues/652)). Register one `avian_host!(ChicoGroveHost)` on the forest / world path. Typed grove `LodScene`s stay registered on [`VegetationViewPlugin`](src/view.rs) for leftover nested hosts.
- [`LayerDropOut::for_stacked`](src/kind.rs) empties Medium/Low/UltraLow for the tufts layer **and** tuft-typed tiles on any layer (Monster / Braid understory). High drops plants whose **blade** height is under [`TUFT_DROP_MIN_HEIGHT_M`](src/kind.rs) (not patch XZ). Do not bake that into individual grove files.
- Forest present is one [`ForestPresenter`](src/present.rs) over a composed [`GroveWorldSource`](src/world.rs). Hide / cull / spawn stay there. `ForestPlugin::<ForestPresenter<S>>` takes the source: [`FlatWorld`](src/world.rs), [`OnTerrain`](src/world.rs)`<H>` (GET stored height; skip grow when missing), or a host wrapper such as Richmond `DevelopmentExclusions`. Growing plants stays presentation, not generate. Do not `get_or_generate` Durham from grove present ([#720](https://github.com/ramate-io/maybraid/issues/720)).
- Hosts that want groves **and** canopy bump-outs register [`VegetationPlugin`](../vegetation-on-terrain-playground/src/vegetation.rs)`<S>` (same `S`). Bump present looks up a terrain mesh by cell size (160 m near / 320 m far) from a [`TerrainMeshSource`](../vegetation-on-terrain-playground/src/bump_out.rs); skip the tick when that cell is not in the store. Do not wrap Durham `TerrainNear` / `Far` plugins as bump presenters.
- A [`ForestIndex`](src/index.rs) stores forests, groves, and [`CanopyBumpOut`](src/bump_out.rs) cells. World presents `ChicoGrove`; the scene stack LOD-refreshes those hosts. Bump-outs are a separate LOD channel. Generate, present, and scene plugins stay independently loaded.
- Default grove stream rings: generate [`GROVE_GENERATE_RADIUS_M`](src/generation.rs) (3 km, selection / recipes only), present [`GROVE_PRESENT_RADIUS_M`](src/generation.rs) (1 km, grown geometry). [`install_forest_stream`](src/stream.rs) stamps [`ForestIndex`](src/index.rs) noise / layering and enables grove bullseyes. Generate / present produce arm keep from the lattice-snapped disk without a full-ring region impulse; drain scans that keep once. Bullseyes stay disabled until install. Canopy bump-outs present a 1–5 km keep AABB ([`BUMP_OUT_INNER_RADIUS_M`](src/bump_out.rs)–[`BUMP_OUT_OUTER_RADIUS_M`](src/bump_out.rs)) and spawn only on existing 160 m fine terrain cells (they clone Durham mesh handles; they do not grow the fine generate disk). Empty forest cells borrow occupied cardinal neighbors; stay empty only when the cell and those neighbors are all `None`.
- Pinned review cells use [`ForestLayering::typical_layers`](src/kind.rs) (highest-weight non-`None` grove per layer). Hopscotch cells still Bucket-Throw.

File shape: no `mod.rs`. Methods live on `ForestExtent`, `ChicoForest`, `ChicoGrove`, `LayeringKind`, and the assemble helpers.
