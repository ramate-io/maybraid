[project]
name = "maybraid-iconography-home"
version = "0.1.0"
description = "Headless Blender home-screen logo bake (TEXT_YELLOW on MENU_CLEAR)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
py-modules = ["main"]
