[project]
name = "maybraid-glb"
version = "0.1.0"
description = "Headless Blender export for Maybraid .blend assets (.glb)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
py-modules = ["main"]
