[project]
name = "maybraid-clothes-fit"
version = "0.1.0"
description = "Headless Blender clothing fit: Catmull-Clark, Outside target-normal wrap, decimate, carve FitTo body, rebind armature"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
py-modules = ["main"]
