[project]
name = "maybraid-clothes-fit"
version = "0.1.0"
description = "Headless Blender clothing fit: inflate along normals, shrinkwrap to a bind-pose body, export garment + armature"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
py-modules = ["main"]
