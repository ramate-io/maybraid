[project]
name = "maybraid-armature-dump"
version = "0.1.0"
description = "Headless Blender armature hierarchy dump for Maybraid character art"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
py-modules = ["main"]
